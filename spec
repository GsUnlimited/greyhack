getUInt8(index:number)->UInt8
{
    //holen uns an stelle index den ersten wert und wandeln in hex um
    //holen uns an stelle index + 1 den zweiten wert und wandeln in hex um
    //kombinieren sie(sauber)
    //konvertieren von hex zu decimal 
    //return decimal
}